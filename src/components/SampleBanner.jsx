export default function SampleBanner() {
  return (
    <div className="sample-banner">
      Showing sample dates. Run <code>npm run import-photos</code> with your Google Takeout export to
      load your real timeline.
    </div>
  );
}
